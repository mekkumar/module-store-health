define([
    'jquery'
], function ($) {
    'use strict';

    return function () {
        /*
         * StoreHealth dashboard is initialized directly
         * from dashboard.phtml.
         *
         * This module is intentionally kept empty to avoid
         * duplicate AJAX requests.
         */
    };
});